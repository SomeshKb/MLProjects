import { AnimationTypingDirective } from './animation-typing.directive';

describe('AnimationTypingDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimationTypingDirective();
    expect(directive).toBeTruthy();
  });
});
